import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Attorney Management section — rendered inside the unified Admin dashboard. */
export default function AdminAttorneysPage() {
  return <AdminDashboard initialSection="attorneys" />;
}
