import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Workload Board — rendered inside the unified Admin dashboard. */
export default function AdminWorkloadPage() {
  return <AdminDashboard initialSection="workload" />;
}
