import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Role Permissions — rendered inside the unified Admin dashboard. */
export default function AdminRolesPage() {
  return <AdminDashboard initialSection="roles" />;
}
