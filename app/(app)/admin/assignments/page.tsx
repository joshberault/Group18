import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Assignments — rendered inside the unified Admin dashboard. */
export default function AdminAssignmentsPage() {
  return <AdminDashboard initialSection="assignments" />;
}
