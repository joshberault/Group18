import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Employee Profiles list — rendered inside the unified Admin dashboard. */
export default function AdminEmployeesPage() {
  return <AdminDashboard initialSection="employees" />;
}
