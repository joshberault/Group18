import { AdminDashboard } from "@/components/admin/AdminDashboard";

/** Admin Matters — rendered inside the unified Admin dashboard. */
export default function AdminMattersPage() {
  return <AdminDashboard initialSection="matters" />;
}
